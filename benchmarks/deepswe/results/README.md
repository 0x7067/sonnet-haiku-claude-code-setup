# DeepSWE Results

Keep concise result summaries here. Do not commit raw `jobs/` directories.

Each summary should include:

- date and benchmark commit;
- Pier version and runner command;
- agent/model;
- task selection;
- score and exceptions;
- cost or token usage if available;
- one or two concrete optimization findings.
